[
  {
    "id": 1,
    "config": "tabWidth",
    "options": [
      {
        "value": 2,
        "description": "Use 2 spaces for indentation.",
        "code": ["  "]
      },
      {
        "value": 4,
        "description": "Use 4 spaces for indentation.",
        "code": ["    "]
      }
    ],
    "default": 2,
    "description": "Number of spaces per indentation level."
  },
  {
    "id": 2,
    "config": "semi",
    "options": [
      {
        "value": true,
        "description": "모든 문장의 끝에 세미콜론을 추가합니다.",
        "code": ["console.log(\"hello\");"]
      },
      {
        "value": false,
        "description": "모든 문장의 끝에 세미콜론을 추가하지 않습니다.",
        "code": ["console.log(\"hello\")"]
      }
    ],
    "default": true,
    "description": "Print semicolons at the ends of statements."
  },
  {
    "id": 3,
    "config": "singleQuote",
    "options": [
      {
        "value": true,
        "description": "Use single quotes for strings.",
        "code": ["console.log('hello');"]
      },
      {
        "value": false,
        "description": "Use double quotes for strings.",
        "code": ["console.log(\"hello\");"]
      }
    ],
    "default": false,
    "description": "Use single quotes instead of double quotes."
  },
  {
    "id": 4,
    "config": "jsxSingleQuote",
    "options": [
      {
        "value": true,
        "description": "Use single quotes for strings.",
        "code": ["'"]
      },
      {
        "value": false,
        "description": "Use double quotes for strings.",
        "code": ["\""]
      }
    ],
    "default": false,
    "description": "Use single quotes instead of double quotes in JSX."
  },
  {
    "id": 5,
    "config": "trailingComma",
    "options": [
      {
        "value": "all",
        "description": "Add a trailing comma where valid in ES5 (objects, arrays, etc.)",
        "code": [","]
      },
      {
        "value": "es5",
        "description": "Add a trailing comma where valid in ES5 (objects, arrays, etc.)",
        "code": [","]
      },
      {
        "value": "none",
        "description": "Remove all trailing commas.",
        "code": [""]
      }
    ],
    "default": "all",
    "description": "여러 줄로 쉼표로 구분된 구문 구조에서는 가능한 한 후행 쉼표를 인쇄합니다. "
  },
  {
    "id": 6,
    "config": "bracketSpacing",
    "options": [
      {
        "value": true,
        "description": "Add a trailing comma where valid in ES5 (objects, arrays, etc.)",
        "code": ["{ foo: bar }"]
      },
      {
        "value": false,
        "description": "Add a trailing comma where valid in ES5 (objects, arrays, etc.)",
        "code": ["{foo: bar}"]
      }
    ],
    "default": true,
    "description": "객체 리터럴의 괄호 사이에 공백을 인쇄합니다."
  },
  {
    "id": 7,
    "config": "bracketSameLine",
    "options": [
      {
        "value": true,
        "description": "Add a trailing comma where valid in ES5 (objects, arrays, etc.)",
        "code": [
          "<button",
          "  className=\"prettier-class\"",
          "  id=\"prettier-id\"",
          "  onClick={this.handleClick}>",
          "  Click Here",
          "</button>"
        ]
      },
      {
        "value": false,
        "description": "Add a trailing comma where valid in ES5 (objects, arrays, etc.)",
        "code": [
          "<button",
          "  className=\"prettier-class\"",
          "  id=\"prettier-id\"",
          "  onClick={this.handleClick}",
          ">",
          "  Click Here",
          "</button>"
        ]
      }
    ],
    "default": false,
    "description": "여러 줄로 된 HTML(HTML, JSX, Vue, Angular) 요소의 >를 다음 줄에 단독으로 두지 않고 마지막 줄 끝에 배치합니다(self closing 태그는 적용되지 않음)."
  }
]
